# GitHub CLI manual

Some useful commands for myself.
I should use `gh` with `git`, which are cli tools for git repository management.
## Configuration
* `gh auth login`
* `gh auth status`
* `gh config set editor <editor>`; I preferred

Create 

About GitHub workflow: repository, issues, pull request, label, gist, project, release, repo

## repo
* `gh repo clone `
* `gh repo create`
* `gh repo fork`
* `gh repo list`
* `gh repo sync`

